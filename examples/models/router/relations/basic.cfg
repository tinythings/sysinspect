relations:
  general-network:
    remote:
      requires:
        - ssh-sockets
    local:
      requires:
        - no-ssh-sockets
    $:
      consists:
        - routes
        - interfaces
        - addresses
        - ssh-sockets
